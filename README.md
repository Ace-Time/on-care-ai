# on-care-ai

## pip install openai
## pip install python-dotenv  // 오픈Key 숨겨줌
## pip install fastapi uvicorn openai python-dotenv pydantic  // fest api


## python -m uvicorn main:app --reload --port 8000  // 실행 (터미널 창에)